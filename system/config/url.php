<?php defined('SYSPATH') OR die('No direct script access.');

return array(

	'trusted_hosts' => array(
		// Set up your hostnames here设置主机名
		//
		// 例子：Example:
		//
		//        'example\.org',
		//        '.*\.example\.org',
		//
		// Do not forget to escape your dots (.) as these are regex patterns.不要忘记逃脱你的点(。)这些都是正则表达式模式。
		// These patterns should always fully match,这些模式应该完全匹配,
		// as they are prepended with `^` and appended with `$`因为它们是前缀与“^”和附加“$”
	),

);
